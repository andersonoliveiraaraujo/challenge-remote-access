ssh -L 1000:clientA:1234 serverA ssh -L 5678:clientA:8000 -N serverB

This will open a tunnel from clientA to serverA and another tunnel from serverA to serverB.

Therefore, when open the addrees http://localhost:8000 in a browser will connected on service port 8000 of serverB.
